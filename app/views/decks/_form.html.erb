<%= form_for(@deck) do |f| %>
  <% if @deck.errors.any? %>
    <div class="alert alert-block alert-error">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <h2><%= pluralize(@deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% @deck.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :title %>
  <%= f.text_field :title, placeholder: "Type title...", class: "input-xxlarge" %>
  <%= f.label :description %>
  <%= f.text_area :description, placeholder: "Type description...", rows: 3, class: "input-xxlarge" %>
  <br />
  <%= f.button "Submit", class: "btn btn-primary" %>
<% end %>
