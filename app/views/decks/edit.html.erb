<div class="row">
  <div class="span12">
    <div class="page-header">
      <h1>Editing Deck</h1>
    </div>
    <%= render 'form' %>

    <%= link_to 'Destroy', @deck, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>

    <%= link_to 'Show', @deck, class: "btn" %>
    <%= link_to 'Back', decks_path, class: "btn"  %>
  </div>
</div>
